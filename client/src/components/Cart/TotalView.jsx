import React from "react";

import { Box, Grid, Typography } from "@mui/material";

const TotalView = ({item}) => {
 
  return (
    <>
     hello from total view
    </>
  );
};

export default TotalView;
